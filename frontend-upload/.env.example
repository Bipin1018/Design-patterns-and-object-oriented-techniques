# Copy to .env.local for local development. Vite only exposes VITE_-prefixed vars.
# Point this at the FastAPI origin; leave the path off (the client adds /health).
VITE_API_BASE_URL=http://localhost:8000
