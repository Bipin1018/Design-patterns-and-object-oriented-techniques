"""Smart greenhouse backend.

Layers:
    domain          business entities and rules (empty in Phase 1)
    application     use cases that orchestrate the domain (empty in Phase 1)
    infrastructure  settings, database engine/session, external systems
    interfaces      delivery mechanisms — HTTP routers live in interfaces/api
"""
